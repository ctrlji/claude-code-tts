---
description: Auto-speak the whole of every Claude response from now on
allowed-tools: Bash(~/.claude/plugins/claude-code-tts/bin/tts-ctl:*)
---

Run this exact command and nothing else:

```
~/.claude/plugins/claude-code-tts/bin/tts-ctl mode full
```

Then tell the user in one short sentence that full responses will now be spoken. Do not take any other action.
